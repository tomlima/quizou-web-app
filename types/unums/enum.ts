export enum QuizStatus {
  Draft = 0,
  Active = 1,
  Deleted = 2,
  Archived = 3,
}

export enum Difficulty {
  Easy = 0,
  Moderate = 1,
  Hard = 2,
}

export enum Roles {
  Standard = "Standard",
  Admin = "Admin",
}
