export enum QuizStatus {
  Draft = "Draft",
  Active = "Active",
  Completed = "Completed",
  Archived = "Archived",
}

export enum Difficulty {
  Easy = "Easy",
  Medium = "Medium",
  Hard = "Hard",
}

export enum Roles {
  Standard = "Standard",
  Admin = "Admin",
}
